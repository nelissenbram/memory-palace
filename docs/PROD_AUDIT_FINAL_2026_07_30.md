# Final status — audit remediation (commit 1aad3fb · 2026-07-30)

Re-verification of the 90 items that were partial/notfixed after the first sweep.
- ✅ now resolved: **65**
- ⚠️ still partial: **18**
- ❌ still notfixed: **1**
- 🔴 regression: **0**
- ⏸️ deferred (backend/product): **3**
_(settings-subscription's ~3 items not re-checked — one agent hit an API error.)_

## Remaining residual polish (⚠️/❌) — cosmetic tails

- **[atrium-relay-board] Count datums built by number+word concatenation — ungrammatical / unpluralized in de/fr/es** (P1, partial) — Active AtriumRelay path now uses interpolated keys — HomeView.tsx:573 relayDatum = t('relay.wingsCount',{count}) · t('relay.roomsCount',{count}) · t('relay.memoriesCount',{count}); tiles at :756/:759/:760/:785 use relay.photosCountN/storiesCountN/recordedCount/sharedCount; keys exist+translated (de.json:4758-4762,5805-5806; fr.json same) and useTranslation.ts:114 interpolates {count}. Word-order + missing 'recorded' fixed. BUT keys are count-invariant single forms ('{count} Erinnerungen'), so singular/plural still not distinguished (e.g. '1 Erinnerungen'), which the prior detail also flagged. Residual raw concat survives only in the dead USE_VILLA_HUB=false branch (HomeView.tsx:867-868).
- **[library-media] Furniture-slot count badges and pills use hardcoded #FFF and sub-ramp fontSizes** (P3, partial) — The #FFF part is resolved: every badge/pill now uses CREAM (#FCFAF5) — e.g. count badge src/components/ui/RoomMediaPanel.tsx:1256 (color: CREAM, fontSize 0.6875rem); a grep of the file finds no remaining hardcoded #FFF. But the 0.6875rem micro-label floor is not fully applied: slot subtitle stays fontSize 0.625rem (line 1272), picker-item label 0.625rem (line 1409), ShowingCounts 0.625rem (lines 414/426), and DisplayedPill slot-count 0.625rem (line 287).
- **[me-achievements] Share-toast detection duplicates share.ts branch logic and misfires on user-cancel** (P2, partial) — Misfire is FIXED: the toast only fires when method==='clipboard' (AchievementsPanel.tsx:289-290), and method is 'clipboard' only when clipboardIsOnlyPath = !hasNativeShare && !hasWebShare (AchievementsPanel.tsx:35,38). When a share sheet exists and the user cancels, share.ts falls through to clipboard and returns true (share.ts:22-37), but the wrapper reports method:'sheet' so NO 'copied' toast fires — the user-cancel misfire is gone. However the recommended refactor was NOT applied: shareAchievement() still returns a bare boolean (share.ts:3), and the native/web-share branch is RE-DERIVED in the caller module via shareAchievementWithMethod (AchievementsPanel.tsx:31-35) rather than returning a discriminated {ok, method} from share.ts. So the branch logic is still duplicated across the two files (share.ts:11,22 vs AchievementsPanel.tsx:31-32), and the source of truth remains split — the fragility the title calls out persists. Bug behavior resolved; recommended de-duplication not done.
- **[tutorial-system] Navigation copy hardcodes both keyboard and touch instructions instead of adapting to the active modality** (P3, partial) — src/messages/en.json:3107 tutorial.steps.navigation.message still crams both modalities into one string ("Use WASD or arrow keys to walk... On mobile, use the joystick to move and swipe to look."). tutorialStore.ts:31 sets a single static messageKey "tutorial.steps.navigation.message" with no modality branch, and the guided-tour renderer calls t(messageKey) directly (no touch/keyboard split). useTouchControls() in MemoryPalace.tsx:126 is only wired to the joystick/TouchControlsOverlay (lines 1313/1316), not to tutorial copy selection. The recommended pattern exists elsewhere (en.json:1585-1586 phase2Desktop/phase2Mobile; CorridorTutorial.tsx:99-106 dStep vs step) but was never applied to this key. Unchanged from prior partial.
- **[settings-sharing] Save can silently fail to publish rooms in never-published wings** (P1, partial) — handleSave now inspects every op: it wraps each publishWing/publishRoom in {id,run}, awaits with .catch(()=>({error:"failed"})), computes isOk() (share-actions return {ok,error}), and only applies the optimistic published flag to succeeded ops, showing tf('publishError') if any failed (page.tsx:152-204). A slug-vs-uuid publishWing UPDATE (.eq('id', input.wingId), share-actions.ts:45) raises a uuid-syntax error -> isOk=false -> error toast, so the SILENT-SUCCESS symptom is fixed. But getMyPublishableContent still returns id=dbWing?.id||w.id (=slug for a never-published wing, share-actions.ts:389) and publishWing does NOT upsert the wings row, so such a wing can never actually be published -> user gets a persistent, unresolvable error. Root cause (data-model: ensure/resolve wing row) unfixed.
- **[settings-sharing] Passcode creation uses a wing id that may be a slug, not a DB uuid** (P1, partial) — handleCreate now has the missing error branch: on !result.ok it calls setToast({message:t('passcodeCreateError'),type:'error'}) (page.tsx:957-961), so the 'button just spins, no toast' symptom is resolved. But selectedWingId is still seeded from content[0].id (=slug for a never-published wing, page.tsx:941), and createPasscode verifies ownership via .eq('id', input.wingId).single() (passcode-actions.ts:46), which returns 'Wing not found' for a slug -> creation still fails for never-published wings. Symptom fixed, root cause (slug vs uuid) remains.
- **[settings-profile] Persona 'Retake / Take quiz' CTA lands on /atrium with no anchor to the quiz** (P2, partial) — CTA now pushes the intended target — page.tsx:724 `onClick={() => { router.push("/atrium?persona=1"); }}` — but no consumer reads it. HomeView.tsx has zero `useSearchParams`/`searchParams`/`window.location.search` usage (grep found none), and AtriumRelay.tsx has no persona-param handling. The quiz open/scroll is driven only by internal `personaExpanded` state (HomeView.tsx:241,1078). So `?persona=1` is inert: the user lands on /atrium but the quiz is not opened or scrolled into view. Recommendation ('have HomeView read it to open + scroll to the quiz') not implemented.
- **[settings-profile] Persona label reads localStorage only — stale after retaking on another device** (P3, partial) — Profile page still hydrates persona from `localStorage.getItem("mp_persona_type")` once on mount (page.tsx:82) and never re-reads on sync — it does not listen for the `mp-settings-synced` event and does not query the profiles row. A cross-device store now exists (settingsSync.ts:23 syncs mp_persona_type into profiles.local_settings), but `syncSettingsFromServer()` is invoked only from MemoryPalace.tsx:442, not from the settings surface — so on a fresh device that hasn't loaded the 3D palace, the settings label stays stale/empty. Improvement over prior 'notfixed' (server persistence + sync now exist), but the settings page itself is not wired to hydrate from it, so the described staleness persists on this surface.
- **[settings-shell] Mobile card polish depends on a brittle inline-style attribute-substring selector** (P2, partial) — layout.tsx:424-425 adds the preferred hook `.mp-settings-mobile-content .mp-settings-card` as the primary selector but keeps `> div > div[style*="border-radius:1rem"]` as fallback. Grep shows `mp-settings-card` appears ONLY in layout.tsx — no sub-page applies the class, while all 8 sub-pages still render inline borderRadius:"1rem" cards (49 occurrences: profile 9, legacy 14, subscription 7, family 6, connections 4, sharing/security/notifications 3 each). The brittle substring selector is therefore still the sole functional matcher; the recommendation to tag cards with the shared class is only half-implemented (target exists, tagging does not).
- **[settings-legacy] Two divergent legacy contact data models coexist (LegacyPanel vs settings page)** (P1, partial) — The 'at minimum' mitigations from the recommendation are DONE but the two divergent models still coexist. Enum reconciled: LegacyPanel now uses canonical 'wings_only' (LegacyPanel.tsx:77-78) and normalizes historical selected_wings/specific_rooms on read (normalizeAccessLevel, LegacyPanel.tsx:81-85), so access-level labels no longer fall through to raw-string display. Cross-vocab display tolerated: contactWingsCount reads both accessible_wings and wing_access (LegacyPanel.tsx:37-40). BUT the two action modules and two column vocabularies are NOT consolidated: track-actions.ts still writes accessible_wings/accessible_rooms (track-actions.ts:174-176, 206-207) while legacy-actions.ts writes wing_access/room_access (legacy-actions.ts:123-124, 220-221) — both to the same legacy_contacts table. Worse, the wing ID spaces remain incompatible (WINGS slugs vs wings-table UUIDs), explicitly acknowledged in-code (LegacyPanel.tsx:31-49); contactPickerWings only pre-selects this panel's native slugs (LegacyPanel.tsx:48-49), so a contact created in the settings page still cannot round-trip its wing selection into the panel's picker. Full consolidation needs a data-model/product decision (retire LegacyPanel or unify columns), hence partial not resolved.
- **[kep-capture] Panel and cards use theme.ts linen/warmStone/sandstone rather than canon CREAM/TRAY tokens** (P3, partial) — KepCapturePanel.tsx defines canon-named locals CREAM/TRAY/HAIRLINE/SAGE (lines 19-23) and the panel/header/card chrome now use them (dialog bg TRAY line 197 'was C.linen'; header gradient NEUTRAL_SOFT->TRAY line 214; code chips TRAY line 382; btnSecondary NEUTRAL_SOFT line 471) — so C.linen/C.warmStone/C.sandstone were removed from the surface chrome. CREAM(#FCFAF5), HAIRLINE(#E3D6BC), SAGE(#56683C) match src/lib/libraryTokens.ts exactly. However the local TRAY='#F2EDE4' (line 21) does NOT match libraryTokens TRAY='#F6EBE3' (libraryTokens.ts:49); #F2EDE4 is exactly the legacy theme.ts linen value (theme.ts:3), so the tray neutral is a renamed legacy value, not the canon token, and tokens are hardcoded local copies rather than imports. warmStone/sandstone still appear only inside decorative SVG illustration fills (lines 68,95,105,114), out of scope for panel/card neutrals.
- **[social-publish] Privacy toggles (Allow download, Show in public palace) start from hardcoded defaults, never seeded from server — mutate wrong baseline** (P1, partial) — SharingPanel.tsx:54-55 still `useState(true)`/`useState(false)`; setAllowDownload/setShowPublicPalace are only called inside the click handlers (lines 390/394/410/413), no mount effect seeds them. fetchRoomShares (sharing-actions.ts:66-70) selects id,shared_with_email,permission,accepted,status,email_sent,invite_message,can_add,can_edit,can_delete,created_at — allow_download is NOT read, and no fetch reads room public visibility. So both toggles show/write the wrong baseline. The error-surfacing half of the recommendation IS fixed (handlers now setError + revert at 393-396/412-415) and the empty-shares divergence is guarded (line 388 blocks toggle when shares.length===0), but the core 'seed from server' defect remains.
- **[social-safety] Copied share LINK is non-functional — lands on an empty passcode form** (P0, partial) — The modal HALF-fixed it: PasscodeModal.tsx:178-182 now builds `${origin}/passcode?code=${encodeURIComponent(createdShare.passcode)}` (was a bare '/passcode'). But the RECEIVING side never reads that param. PasscodeEntry.tsx initializes `code` to "" (line 14) and its only useEffect merely focuses the input (lines 18-20); a Grep for searchParams/location.search/URLSearchParams/useSearchParams in PasscodeEntry.tsx returns no matches. The route wrapper src/app/passcode/page.tsx:9-11 renders `<PasscodeEntry />` with no props and no `searchParams` argument. So a recipient clicking the shared link still lands on a blank field with no pre-fill and no auto-validate — recommendation part 2 (read the ?code= param on mount) is unimplemented.
- **[social-reactions] Failed fetch leaves both panels stuck on the loading state forever** (P1, partial) — SharedWithMePanel.tsx is now fully fixed: getAcceptedShares() has .catch(()=>setError(true)) + .finally(()=>setLoading(false)) with a cancelled guard (src/components/ui/SharedWithMePanel.tsx:54-66), plus an error UI and retry button (lines 118-142). BUT the finding's detail/recommendation explicitly cover the second panel, and InviteNotificationsPanel.tsx still has the identical bug: getPendingInvites().then(result => { setInvites(...); setLoading(false); }) with NO .catch() and setLoading(false) only inside the .then (src/components/ui/InviteNotificationsPanel.tsx:42-47). A rejected getPendingInvites() pins that panel on 'loading...' forever with no recovery.
- **[auth-recovery] Wing-only passcode shares 404 at the destination (/public/[slug] assumes a room)** (P1, notfixed) — The wing-only branch was added only to getShareData() in page.tsx (lines 58-72), but that function feeds ONLY generateMetadata (line 101). The visible page renders <PublicGallery slug> (page.tsx line 135), which fetches /api/public-share/[slug]/route.ts. That route still does rooms.eq('id', share.room_id).single() and returns 404 'Room not found' when room_id is null (route.ts lines 57-65). Passcode still validates 'Valid' then dead-ends at the actual gallery for a wing-only share.
- **[public-profile] Video thumbnails on public gallery cards autoload full video source with no poster fallback** (P2, partial) — Grid now renders an <Image> poster from mem.thumbnailUrl and only mounts <video> in the lightbox (PublicGallery.tsx:431-439 vs 622-642) — the autoload regression is fixed. But /api/public-share never selects or maps thumbnail_url (route.ts:78 select, 102-112 response), so mem.thumbnailUrl is always undefined and the poster never renders (falls back to HSL wash + play badge).
- **[landing] Hero/scrim/shadow colors hardcoded as raw rgba umber instead of tokens** (P3, partial) — The core of the finding is addressed: theme.ts now defines L.scrim.hero/L.scrim.final (src/lib/theme.ts:114-119) and L.shadow.cta/L.shadow.ctaHero (src/lib/theme.ts:120-123), and LandingV2Client consumes them — hero wash via L.scrim.hero (LandingV2Client.tsx:337), CTA shadows via L.shadow.cta (LandingV2Client.tsx:206) and L.shadow.ctaHero (LandingV2Client.tsx:1109). However the recommendation (derive from the warm-ink SHADOW ramp; stop hand-typing umber rgba) is only partly met: raw rgba warm-ink/gold values are still hardcoded, merely lifted into local consts LSHADOW/LSCRIM/LGLOW that are NOT theme tokens — ctaHover 'rgba(107,51,24,0.35)' / ctaActive 'rgba(107,51,24,0.25)' (LandingV2Client.tsx:62-63), toggle scrim 'rgba(36,28,21,0.55)' + 'rgba(252,250,245,0.5)' (LandingV2Client.tsx:67-69), and the ceremonial gold glow ramp 'rgba(212,175,55,...)' (LandingV2Client.tsx:74-79). Additional inline raw rgba remain at call sites: hero H1 textShadow 'rgba(36,28,21,0.45)' (LandingV2Client.tsx:1072) and hero sub color 'rgba(252,250,245,0.92)' (LandingV2Client.tsx:1082). None derive from the shared SHADOW ramp.
- **[legal] Off-canon background, ink and interactive colors throughout the legal surface** (P1, partial) — privacy/page.tsx (lines 29,31,113,569) and terms/page.tsx (60,62,143,346) now use canon C.cream/C.ink/C.ember/C.inkMuted — fully aligned. But the detail also cites data-deletion, which is still off-canon: data-deletion/page.tsx:22,153,172,220,230 paint body/list/footer text with C.walnut (#8B7355 brown) and C.muted, and line 133/243 use C.terracotta (#C66B3D) for the eyebrow and links instead of C.ember.
- **[legal] Legal pages ignore useIsMobile/useIsCompact and rely on clamp()/vw for responsiveness** (P3, partial) — privacy/page.tsx:6,19-20 and terms/page.tsx:6,50-51 now use useIsMobile/useIsSmall + rem + 2.75rem nav targets. But data-deletion/page.tsx imports no responsive hook at all (imports end at line 10 with no useIsMobile) and has no MobileCtx/isMobile branching, so it still relies solely on clamp()/vw.

## Deferred — need product/backend decision (bucket B)

- **[kep-rooms] NavigationBar highlights 'Atrium' while on the Keps page** — src/app/(app)/palace/keps/page.tsx still passes currentMode="atrium" (lines 206 desktop, 466 mobile) with activeTab={null}. NavigationBar.tsx line 529 (mobile) and line 813 (desktop) resolve isActive to mode===currentMode when no activeTab is set, so the Atrium tab does read as active. Neither recommended option was applied; instead lines 203-204 add an explicit comment declaring this intentional ("we keep Atrium as the active mode here rather than introducing a separate Kep tab"). Unchanged behavior, now a documented product decision.
- **[auth-recovery] reset-password re-hardcodes the entire auth shell instead of sharing it** — reset-password/page.tsx still defines its own wrapperStyle (lines 319-331), cardStyle (lines 333-345) and AuthBlobs() (lines 281-314) rather than importing a shared AuthShell. Comment at lines 274-280 explicitly defers full extraction as needing the (auth)/layout + a new shared component 'outside this pass's editable-file allowlist' — a structural refactor decision.
- **[auth-recovery] reset-password duplicates the (auth) layout chrome instead of living in the route group** — File is still at src/app/reset-password/page.tsx (outside the (auth) route group) and duplicates safe-area/responsive chrome locally (wrapperStyle lines 319-331, cardStyle 333-345). Moving it under (auth)/ or extracting a shared wrapper is explicitly deferred per the comment at lines 274-280; it is a route-structure decision, not a bug in current code.
