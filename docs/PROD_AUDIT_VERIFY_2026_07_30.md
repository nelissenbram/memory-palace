# Production Audit — Verification (commit 4ae736f · 2026-07-30)

Independent read-only re-check of all 906 audit findings against live code.

- ✅ resolved: **811**
- ⚠️ partial: **78**
- ❌ notfixed: **12**
- 🔴 regression: **0**
- ⏸️ deferred: **5**

## ❌ Not fixed (still present)

- **[atrium-nav] PalaceSubNav room breadcrumb pill renders roomName prop raw (not translated) while other room labels use translateRoomName** (P2) — renderRoomPill still renders the raw `roomName` prop directly (PalaceSubNav.tsx:682) — no nameKey/RoomItem is threaded through and translateRoomName is not applied to the breadcrumb, unlike dropdown entries (l.648).
- **[explore-social-cards] PalaceCard.tsx is dead code — never imported or wired** (P2) — Grep shows src/components/social/PalaceCard.tsx is not imported anywhere; ExplorePageClient.tsx:659 defines its own local PalaceCard and imports the landing PalaceCard, not this one — still orphaned.
- **[settings-sharing] Family-share passes slug where server/DB likely expects a wing id; wingLabel double-guesses** (P2) — shareWing is still called with shareWingSlug (page.tsx:542) and wingLabel still does the dual slug||id match (line 531: w.slug===wingSlug || w.id===wingSlug); no single canonical identifier was adopted end-to-end.
- **[settings-profile] Persona label reads localStorage only — stale after retaking on another device** (P3) — personaType still hydrated solely from localStorage.getItem('mp_persona_type') (page.tsx:82); load() query (page.tsx:111-135) reads no persona column from profiles, so cross-device staleness persists.
- **[import-hub] Cloud import progress is faked: single batch POST reports totals but no per-item progress and no timeout** (P3) — CloudImportPanel.tsx handleImport (L275-333) is still a single fetch POST with no AbortController, no timeout, and no Cancel button (grep for AbortController/setTimeout/signal/Cancel returns no matches); the bar remains a purely animated indeterminate stripe (L565-595), so a stalled cloud import is unrecoverable.
- **[import-hub] ImportHub cloud-provider tab strip and MassImport source toggle can overflow without horizontal scroll on mobile** (P3) — The two strips named in the finding were not given scroll: ImportHub cloud providers render as a fixed grid 1fr 1fr 1fr with no overflowX (ImportHub.tsx L694-698); MassImportPanel source toggle is display:flex with no overflowX (L405). Only CloudImportPanel's own provider tabs got overflowX:'auto' (L380).
- **[kep-capture] Panel and cards use theme.ts linen/warmStone/sandstone rather than canon CREAM/TRAY tokens** (P3) — KepCapturePanel.tsx still sources neutrals from theme.ts — C.linen (185,202), C.warmStone (202,459), C.sandstone (via btn styles); not migrated to libraryTokens. (Visually moot since theme.ts was canon-reskinned, but the literal recommendation was not applied.)
- **[auth-recovery] reset-password re-hardcodes the entire auth shell instead of sharing it** (P3) — reset-password/page.tsx:204-230 still hand-rolls its own wrapperStyle + cardStyle (gradient, blurred cream card) rather than sharing an AuthShell with the (auth) route group; the file still lives outside src/app/(auth)/.
- **[auth-recovery] reset-password relies on getUser() to detect the recovery session, but a normally logged-in user gets the reset form too** (P2) — reset-password/page.tsx:32-36 still calls supabase.auth.getUser() and sets hasSession=!!user, treating any authenticated session as a valid reset context — no ?type=recovery flag or AAL/AMR check was added.
- **[auth-recovery] reset-password duplicates the (auth) layout chrome instead of living in the route group** (P3) — reset-password/page.tsx still sits at src/app/reset-password/ (not under src/app/(auth)/) and re-implements its own wrapper/card/safe-area chrome (lines 204-230) rather than inheriting the shared (auth) layout.
- **[public-profile] Enter-Palace button assumes publishedWings[0] ordered by published_at, not curated** (P3) — ProfilePageClient.tsx:156 still routes to `/visit/${profile.id}/${publishedWings[0].slug}`; no palace-level entry or owner-marked default landing wing mechanism added.
- **[pricing] Currency selection is not passed to Stripe checkout — user charged in server default** (P2) — Client sends currency (page.tsx line 138) but src/app/api/stripe/checkout/route.ts destructures only `{ plan, interval }` (line 36) and never references `currency` (grep: no matches). User still charged in the Stripe price's default currency; no 'approximate/billed in EUR' notice on card either.

## ⚠️ Partial (fix incomplete)

- **[atrium-relay-board] Count datums built by number+word concatenation — ungrammatical / unpluralized in de/fr/es** (P1) — wings/rooms/memories datum and interviews now use interpolated keys — t('relay.wingsCount',{count}) etc. (HomeView 573, 718-719), and t('relay.recordedCount',{count}) (line 760). But the photo/story tile datums still hand-concatenate: `${mtc.photo} ${t('relay.photosCount')}` (line 756) and `${mtc.story} ${t('relay.storiesCount')}` (line 759), where photosCount='photos'/'Fotos' is a bare noun forced into English number-noun order.
- **[atrium-relay-board] Ledger warmth-strip dots + ledger accent use ochre #8A6410, an off-token gold** (P2) — AtriumRelay line 98 names LEDGER_OCHRE = '#8A6410' with a documented rationale (text-safe gold on cream that ceremonial GOLD #D4AF37 fails), and the ledger dots use FLAME_GOLD #E8C255 (lines 425,433). The recommendation to promote these to named tokens is met locally, but #8A6410/#E8C255 are still declared in AtriumRelay rather than exported from libraryTokens, so they are not shared canon tokens.
- **[atrium-relay-board] EmbersRow avatars are undersized for a comfortable touch target** (P2) — EmbersRow button (AtriumRelay line 356) now sets minHeight: '2.75rem' and padding '0.35rem 0' with a 3.25rem cameo, meeting the >=2.75rem tap zone. However the recommended edge-fade / aria-describedby scroll hint on the overflow-x strip (line 346) is not present.
- **[atrium-nav] Sub-nav and bottom bar mix px borders instead of the canon rem hairlines** (P2) — Bar/pill/dropdown hairlines converted to rem (e.g. 0.0625rem l.200,224,384,401), but several 1px literals remain: quick-nav pill l.513, dropdown divider l.599, Publish/Passcode buttons l.841/878/1202/1233; MobileBottomBar still uses `1px solid` (l.209,237,270).
- **[atrium-nav] MobileBottomBar and TopBar mobile menu rely entirely on emoji glyphs for iconography, breaking consistency with the crafted SVG icon system** (P2) — MobileBottomBar fully migrated to a crafted stroke-SVG ActionIcon set (MobileBottomBar.tsx:20-67, used throughout), but TopBar's mobile menu still uses emoji glyphs (TopBar.tsx:241,262,282,290,299) — the finding names both surfaces.
- **[atrium-nav] Shared-wing display name is fabricated from the slug instead of the real name** (P2) — resolveSharedWingName maps canonical wing slugs through translateWingName (TopBar.tsx:31-35), but a real custom display name is still NOT threaded through SharedWingItem (no name field added; interface l.50-56); custom-named wings still fall back to a title-cased slug per the function's own comment.
- **[atrium-nav] Shared-wing names shown untranslated/raw-cased instead of via translateWingName** (P2) — Shared wings now resolve via resolveSharedWingName → translateWingName for canonical slugs in both mobile menu (TopBar.tsx:253) and WingsDropdown (l.1081), but non-canonical (custom-named) wings still show a title-cased raw slug (l.34) since no real name is fetched.
- **[library-sidebar] colorPicker and tooltip open-state never auto-closes on outside click / wing switch** (P3) — handleSelectWing (lines 113-117) now resets colorPickerWing=null and tooltipOpen=false on wing switch, and the tooltip is click-toggle on touch / hover on desktop (lines 434-436) rather than both. However there is still no outside-click / click-away listener, so tapping elsewhere (without switching wings) leaves the picker open — the second half of the recommendation is unaddressed.
- **[library-media] Furniture-slot count badges and pills use hardcoded #FFF and sub-ramp fontSizes** (P3) — Pure #FFF replaced by CREAM and count badge fontSize raised to 0.6875rem (1219); however DisplayedPill toast still uses fontSize 0.5rem (326) and the picker 'assigned' label uses 0.5rem (1379), below the RT.overline 0.6875rem floor.
- **[library-media] Sort and display-unit dropdowns lack menu/listbox roles and aria state** (P2) — Triggers now have aria-haspopup='menu' + aria-expanded (192-193, 882-883), popovers role='menu' (218, 899), options role='menuitemradio' + aria-checked (242-243, 911-912); but no arrow-key handling is wired on either popover, which the recommendation also requested.
- **[library-media] RoomMediaPanel adapts only via useIsMobile — no iPad-portrait / compact handling** (P2) — useIsTablet now imported and used to keep the close button at 2.75rem on tablet (7, 439, 790); but pills/other targets are not tablet-scaled and no useIsCompact is used, so iPad density is only partially addressed.
- **[explore-social-cards] Neither card uses responsive hooks; layout is prop-driven only** (P2) — ProfileCard.tsx:49-50 now uses useIsCompact() merged with prop (compactLayout); PalaceCard.tsx still has no responsive hook (only useReducedMotion), so this is fixed only for ProfileCard.
- **[explore-social-cards] No dedicated empty/loading/error styling for absent avatar/data** (P3) — PalaceCard.tsx:39-86 adds PalaceCardSkeleton (tray shimmer + reduced-motion guard) and initial '?' fallback (:141-142); ProfileCard.tsx has a '?' fallback (:121) but still no skeleton/loading state of its own.
- **[explore-social-cards] Private-profile ProfileCard exposes a Follow button on a zeroed profile** (P3) — profile-actions.ts:26,67 adds is_limited flag on the private branch, but ProfileCard.tsx:203 still renders Follow on `!profile.is_own` without reading is_limited, so the button/zeroed stats still show on a limited profile.
- **[me-achievements] Share-toast detection duplicates share.ts branch logic and misfires on user-cancel** (P2) — Misfire is fixed: AchievementsPanel.tsx lines 266-274 detect clipboardIsOnlyPath and only toast 'copied' when no native/web sheet exists. But shareAchievement (share.ts:3) still returns Promise<boolean>, not the recommended discriminated {ok,method} result, so branch logic is still re-derived in the caller.
- **[tutorial-system] Navigation copy hardcodes both keyboard and touch instructions instead of adapting to the active modality** (P3) — The old key `tutorial.steps.navigation.message` (en.json:3096) still crams WASD + mobile joystick into one string, but it is now dead (only TUTORIAL_STEPS in tutorialStore references it, and no live component renders it). The active tour uses modality-split keys phase2Desktop (en.json:1580) vs phase2Mobile (en.json:1581), so the live UX is fixed; the stale combined key just wasn't cleaned up along with the removed overlay.
- **[tutorial-3d] Highlight ring & pulse hardcode gold rgba, not canon focus token** (P1) — Ring border now uses canon token: `0.1875rem solid ${GOLD}` (CorridorTutorial.tsx:177), a legitimate ceremonial focus use. However the @keyframes pulse still hardcodes rgba(212,175,55,0.4) instead of deriving from GOLD (CorridorTutorial.tsx:152, PalaceExteriorTutorial.tsx:186, EntranceHallTutorial.tsx:150) — inline CSS keyframes can't reference a JS const, so the numeric gold literal remains but matches #D4AF37.
- **[tutorial-3d] Highlight cutout radius uses raw px and inline literals, breaking rem discipline** (P2) — EntranceHallTutorial.tsx:111-112 now defines rRem=0.875 and computes r=remToPx(rRem); border uses `${rRem}rem` (line 174) — resolved there. But PalaceExteriorTutorial.tsx:141 still uses raw px radii (0/16/12) and `${r}px` (line 195,210), so the referenced pattern persists in the exterior tutorial.
- **[tutorial-3d] Four exported auto-open hooks are dead code that duplicate (and diverge from) the live path** (P2) — CorridorTutorial.tsx (the referenced file) no longer exports any auto-open hook — good. But usePalaceExteriorTutorial (PalaceExteriorTutorial.tsx:337), useEntranceHallTutorial (EntranceHallTutorial.tsx:295) and useRoomTutorial (RoomTutorial.tsx:180) are still exported and unused; the live auto-open path is inline in MemoryPalace.tsx:615-692, which does NOT implement the ?palaceTour/entranceTour URL-replay reset those hooks contain, so two divergent implementations remain.
- **[tutorial-3d] Corridor/entrance/room highlight cutout intercepts clicks meant for the highlighted control** (P3) — CorridorTutorial.tsx:186-200 adds a transparent button over the highlighted rect that calls advance() on tap (feels interactive, per the recommendation). However EntranceHallTutorial, PalaceExteriorTutorial and RoomTutorial still let a tap on the cutout scrim fall through to onClose (dismiss) with no advance-on-highlight button, so the fix is only applied to the corridor.
- **[tutorial-3d] tipTop/tipLeft positioning uses hard-coded pixel offsets, ignoring rem/font scale and landscape height** (P2) — EntranceHallTutorial.tsx:123-139 now derives cardH/edge/gap from remToPx() and uses them for placement — resolved there. But PalaceExteriorTutorial.tsx:152-174 and CorridorTutorial.tsx:123-135 still use hard-coded pixel offsets (80, 200, 220, 180, 60, 16, 12), so the pattern persists in two of the three positioned tutorials.
- **[tutorial-3d] Highlight cutout mask uses px radius while rest of surface is rem — minor unit inconsistency** (P3) — Corridor and Entrance now define rRem in rem and derive the SVG rx via remToPx(rRem) with borderRadius `${rRem}rem` (CorridorTutorial.tsx:112-113,176; EntranceHallTutorial.tsx:111-112,174). The referenced CorridorTutorial is fixed, but PalaceExteriorTutorial.tsx:141,195,210 still uses raw px radii, so the inconsistency remains app-wide.
- **[tutorial-menu] /help nav back-button and select borders use ad-hoc alpha suffixes rather than a token step** (P3) — help/page.tsx still uses varied ad-hoc alpha suffixes on C.sandstone (e.g. `${C.sandstone}40` line 101, `${C.sandstone}60` lines 108/124/266, `${C.sandstone}80` line 321, full C.sandstone line 163) — not consolidated to one or two canonical hairline steps.
- **[onboarding-wizard] Landscape scroll fix applies only to setup cards, not the full-screen phases** (P2) — Paywall overlay now gets alignItems:flex-start + overflowY:auto + padding when isLandscapePhone (OnboardingWizard.tsx:973-976), but the celebration full-screen phase delegates to the OnboardingCelebration component (line 935) with no landscape overflow treatment applied in this file — celebration's tall-viewport reachability is not addressed here.
- **[settings-security] deleteConfirmDescription bold-split silently drops text if placeholders missing** (P3) — page.tsx:754-768 now falls back to the raw string when parts.length < 3 (comment + early return raw), fixing the silent text drop; however the recommended locale test asserting {boldStart}/{boldEnd} exist per locale was not found, so the guard is code-only without the regression test.
- **[settings-security] ExportPanel hardcodes 1px borders and raw px sizes instead of rem** (P3) — ExportPanel.tsx now sizes paddings/radii/fonts/minHeight in rem throughout, but still uses raw `1px solid ${HAIRLINE}` borders (e.g. lines 576, 617, 863, 911) — those are true hairlines the recommendation permits keeping at 1px, so the remaining px are the sanctioned exception rather than an unaddressed defect.
- **[settings-family] Off-token surface hexes: disabled #EEE9DF, tray #F6EBE3, medallion border #E7D9C4** (P2) — TRAY (#F6EBE3, libraryTokens.ts:49) and CARD_BORDER (#E7D9C4, libraryTokens.ts:54) are now imported shared tokens; but DISABLED='#EEE9DF' is still a surface-local const declared inline at page.tsx:12 rather than promoted to the shared token file.
- **[settings-family] Resend invite is a non-atomic delete-then-recreate that can lose the invite** (P2) — Still a client-side cancel-then-recreate (page.tsx:192-226); mitigations added (checks cancel error before deleting, captures role, attempts restore on recreate failure with resendFailedLost toast), but it remains non-atomic — a crash between cancel and restore can still drop the row. No idempotent server action as recommended.
- **[settings-family] getAllFamilyGroups does per-group N+1 queries and blocking auto-repair inserts on load** (P3) — N+1 fixed: groups/members batch-fetched via two .in(...) queries in Promise.all (family-actions.ts:498-509). But auto-repair inserts still run inline (awaited) on the hot read path (family-actions.ts:471-480) rather than being moved off it — batched into one insert but not off-path.
- **[settings-sharing] Save can silently fail to publish rooms in never-published wings** (P1) — handleSave now inspects per-op results (page.tsx:161-177 isOk/failedWings/failedRooms) and only optimistically flips succeeded targets + shows publishError on anyFailed. BUT publishWing (share-actions.ts:38-48) is still UPDATE-only (no upsert), and getMyPublishableContent (line 389) still passes id=slug for wings with no DB row — such a publishWing UPDATE matches 0 rows yet returns {ok:true,error:undefined}, so isOk()==true and it is still falsely marked published. Error surfacing fixed; silent slug no-op remains.
- **[settings-sharing] Passcode creation uses a wing id that may be a slug, not a DB uuid** (P1) — The missing error UI is fixed: PasscodeSection now shows a toast on !result.ok (page.tsx:953) and on catch (956). But selectedWingId still seeds from content[0].id (line 934 = dbWing?.id||slug) and createPasscode does .eq('id', wingId) (passcode-actions.ts:46), so for a never-persisted wing creation still fails — root slug/uuid mismatch not resolved, only the error toast was added.
- **[settings-sharing] Sharing hardcodes literal accent hexes instead of imported tokens** (P2) — page.tsx:8 now imports EMBER, EMBER_GLYPH, HAIRLINE, CREAM, INK, MUTED, SAGE and references them broadly. However literal accent hexes still recur (e.g. gradient '#B85C38','#9A4F2A' lines 386/712/1082, '#FBF2EC' washes, '#FFFFFF' pills line 638/687, '#C05050' error). Token import done; hardcoded hexes only partially eliminated.
- **[settings-sharing] Family-share permission badge shape inconsistent with the badge system** (P3) — The myShares permission chip now uses canon pill radius 2rem (page.tsx:786), but the sharedWithMe permission chip still uses borderRadius '0.25rem' (line 883), so the badge shape is still inconsistent in one branch.
- **[settings-sharing] SharingSettingsPanel 'received' badges hardcode viewOnly=true, ignoring actual permission** (P3) — The viewOnly badge is no longer a literal true — it is now enabled={share.status==='accepted'} (SharingSettingsPanel.tsx:1014, 1123). It reflects acceptance state rather than a hardcoded true, though it is still not derived from an actual view-permission field.
- **[settings-profile] Profile canon tokens hardcoded as raw hex instead of imported** (P2) — Tokens INK/MUTED/HAIRLINE/EMBER/EMBER_GLYPH/TRAY/CREAM/SAGE now imported from libraryTokens (page.tsx:18) and used widely, but many raw hexes remain inline (e.g. '#B85C38' switch backgrounds page.tsx:621,903,1116; '#E3D6BC','#403B36','#716A5E','#F6EBE3','#FCFAF5' throughout).
- **[settings-profile] Persona 'Retake / Take quiz' CTA lands on /atrium with no anchor to the quiz** (P2) — CTA now router.push('/atrium?persona=1') (page.tsx:724); the query param is passed but this verification did not confirm HomeView reads persona=1 to open+scroll to the quiz — the receiving-side handling is the load-bearing half.
- **[settings-profile] Daylight time-of-day range input has a sub-2.75rem touch target** (P2) — Range input given minHeight:'2.75rem' (page.tsx:1185) so the row is tappable, but no enlarged ::-webkit-slider-thumb was added, so the actual draggable thumb hit-area remains small.
- **[settings-shell] Hardcoded white/cream hex literals instead of tokens (shell)** (P3) — #FCFAF5 literals are gone (CREAM token used on lines 180/216 etc.), but layout.tsx line 291 still uses raw `background: "#FFFFFF"` for the desktop sidebar card — the recommended warm-white token was not introduced.
- **[settings-shell] Mobile card polish depends on a brittle inline-style attribute-substring selector** (P2) — layout.tsx lines 413-415: a shared `.mp-settings-card` class was added to the mobile override, but the fragile `div[style*="border-radius:1rem"]` / `border-radius: 1rem` substring selectors are still retained alongside it.
- **[settings-legacy] Two divergent legacy contact data models coexist (LegacyPanel vs settings page)** (P1) — LegacyPanel still writes via track-actions using accessible_wings (track-actions.ts l.175,206) and is still mounted twice in MemoryPalace.tsx (l.1168,1594); it now normalizes to wings_only and reads both column vocabularies (contactWings/normalizeAccessLevel), but the two action modules + column names (accessible_wings vs wing_access) are not consolidated.
- **[settings-subscription] Subscription raw hex literals everywhere instead of importing tokens** (P2) — Tokens INK/MUTED/HAIRLINE/EMBER/EMBER_GLYPH/CREAM/TRAY/SAGE are imported and used (line 14), but many ad-hoc rgba() literals remain inline for accents/shadows (e.g. 'rgba(154,79,42,0.07)' lines 863,1043; shadow strings lines 482,782); not all hex/rgba routed through tokens.
- **[settings-subscription] Ember CTA gradient duplicated ~10x inline** (P3) — A shared emberCta() helper + EMBER_GRADIENT constant now exist (lines 22-42) and are used for the manage/upgrade primaries (e.g. lines 643,676,734), but several CTAs still inline the gradient/disabled ladder manually (lines 1083-1087, 1259-1264, 1432-1435) rather than reusing emberCta().
- **[settings-subscription] Subscription responsive layout relies almost entirely on isMobile** (P2) — useIsPortrait is now imported and drives the plan-card header wrap/direction (lines 62,488-493,520), and the header row has flexWrap/gap; but the full-comparison grid still collapses purely on isMobile (line 1122) rather than a portrait/narrow-landscape check, so iPad split-view edge remains unaddressed.
- **[palace-touch] Auto-dismiss on a 5s timer with no reduced-motion / cognitive pacing accommodation** (P2) — TouchControlsOverlay.tsx:36-40 timeout now 6s/12s under reduced-motion and only setVisible(false) (LS persists solely on explicit dismiss, line 45) — but the timer is not paused while the overlay has focus, one part of the recommendation.
- **[palace-touch] Joystick stays visible during room tour even while blocking panels are open** (P3) — MemoryPalace.tsx:1318 visible guard now `&& !showUpload && !showSharing`, but the recommended moreMenuOpen term is absent — no such state exists in MemoryPalace.tsx, so upload/sharing panels are covered while any 'more' menu is not.
- **[palace-touch] Raw pixel values in SVG diagram + mixed px units** (P3) — TouchControlsOverlay.tsx:126,152 icon wrappers now sized in rem (1.25rem) with SAGE/EMBER strokes; but the SVG internals still use px viewBox coords/width/height='20' — cosmetic and within-viewBox, so mixed-unit note is largely but not fully retired.
- **[palace-corridor] handleCreate has no guard on croppedFace failure — silently builds from un-cropped preview; objectURL leak** (P2) — objectURL leak fixed (revoke on replace line 74 and unmount line 234), but handleCreate still falls back to `croppedFace || preview!` (line 125) with no guard when cropping failed — an un-cropped preview is still used silently.
- **[import-hub] Heavy emoji usage breaks the Tuscan line-art icon language ImportHub established** (P1) — Primary affordances are now SVG line-art (MassImportPanel BoxGlyph/DownloadGlyph/SparkGlyph/ClipboardGlyph L41-98; UploadPanel UploadGlyph/CameraGlyph/PinGlyph/GlobeGlyph L23-56). Residual emoji remain in MassImportPanel DISPLAY_TYPES select-option glyphs (L27-31 used at L871), the edit pencil button (L816), and the exif chip line (L897-899); CloudImportPanel PROVIDER_META icons and FileList icons are still emoji (L72-77, L991-997).
- **[import-hub] ImportHub cloud provider brand-color SVGs sit against the muted Tuscan palette** (P3) — ImportHub.tsx L737-746 now frames each brand mark in a hairline cream pill (badge treatment) as recommended. CloudImportPanel was NOT aligned to the same treatment — its provider tabs still render bare PROVIDER_META emoji icons with no pill framing (L403).
- **[kep-capture] StepReview uses emoji check/cross and emoji source glyphs instead of canon iconography** (P2) — Check/cross are now canon SVGs — CheckMark (stroke #56683C SAGE) and CrossMark (stroke #716A5E MUTED) at lines 369-384, used at 407 — but the source/icon glyphs remain raw emoji (SOURCE_ICONS emoji rendered at 221 and data.icon at 396).
- **[kep-rooms] Emoji glyphs used for empty/loading state and media icons instead of canon SVG iconography** (P2) — Empty-state emoji replaced by warm SVG shield (pending/page.tsx:110-113), but media-type glyphs remain emoji: route-helpers.ts:48-54 returns 🖼️/🎬/🎤/📝/📄/📎, rendered at pending/page.tsx:167, and kep_icon emoji shown at :174.
- **[kep-rooms] NavigationBar highlights 'Atrium' while on the Keps page** (P3) — keps/page.tsx:210 and :470 pass activeTab={null} (no primary tab reads active), but currentMode='atrium' is still passed intentionally (comment :203-204); Atrium mode remains the current mode, so the sub-route is not fully de-highlighted.
- **[family-tree-shared] Not-found page bypasses i18n system, hardcodes wrong fonts, and is off-canon** (P1) — page.tsx:44-56 fonts/colors are now canon (T.font + CREAM/INK/MUTED), but strings still come from inline notFoundTitle/notFoundDesc Record maps (:8-22) with locale sliced to 2 chars (:34) rather than serverT/shared message files.
- **[family-tree-shared] Relationship rows and notes block use cream as a divider/fill, reducing contrast and rhythm** (P3) — Relationship row dividers now use HAIRLINE (SharedTreeView.tsx:653); the notes block was removed entirely (:615-616) so the TRAY fill recommendation is moot — dividers fixed, notes-fill no longer applicable.
- **[family-tree-shared] Not-found fallback uses wrong fonts/colors and a separate hardcoded string map instead of i18n + design canon** (P2) — page.tsx fonts/colors are now canon tokens, but the not-found strings still live in inline Record maps (:8-22) instead of serverT/getServerLocale from family-tree-actions as recommended.
- **[family-tree-shared] Canvas relies on useIsMobile only; no landscape/iPad-specific tuning and fixed 100vh** (P2) — Shell now uses height:'100dvh' (SharedTreeView.tsx:363, and 100dvh in page.tsx:43), fixing the fixed-vh concern; however it still uses useIsMobile (:42) rather than useIsCompact for the sheet-vs-modal switch and fitToView pad is a fixed 60px (:134).
- **[family-tree] Loading and share-loading states are unstyled bare text / ellipsis** (P2) — Bare '...' replaced with localized labels t("loadingTree") (page.tsx:2051) and t("shareLoading") (:1848), but no branded spinner/skeleton/TreeBranchIcon pulse was added as the recommendation asked.
- **[social-publish] Privacy toggles (Allow download, Show in public palace) start from hardcoded defaults, never seeded from server — mutate wrong baseline** (P1) — SharingPanel.tsx:54-55 allowDownload still useState(true) and showPublicPalace useState(false) with no effect seeding them from server (fetchRoomShares/fetchPublicShare never populate these). Empty-shares click is now guarded (line 388 setError noCollaboratorsForDownload), but the wrong-baseline seeding defect remains.
- **[social-publish] Publish/QR modals skip responsive hooks used elsewhere on the surface** (P3) — PublishModal.tsx:7,20 now imports/uses useIsMobile() for padding (line 164). QRShareModal.tsx does NOT adopt useIsMobile/useIsCompact — no responsive hook present; it relies on fixed rem sizing only. Half the recommendation is done.
- **[social-publish] SharingPanel 'QR' button label is hardcoded, not translated** (P2) — SharingPanel.tsx:317 aria-label now routed through t('shareViaQR') (key exists in all 5 locale files), but the visible label text 'QR' (line 319) is still a hardcoded literal — the 'ideally visible label' half is unaddressed (QR is a brand-ish acronym, arguably acceptable).
- **[social-safety] Copied share LINK is non-functional — lands on an empty passcode form** (P0) — PasscodeModal.tsx:178-182 now embeds `?code=${createdShare.passcode}` in shareUrl, but PasscodeEntry.tsx (11-21) still never reads the param: `code` state initializes to "" and the only useEffect just focuses the input — so a clicked link still lands on a blank field. Only half the recommendation (embed) is done; the read/pre-fill half is missing.
- **[social-reactions] Failed fetch leaves both panels stuck on the loading state forever** (P1) — SharedWithMePanel.tsx:54-64 now has .catch(setError)+.finally(setLoading(false)) plus a retry button (lines 124-141), but InviteNotificationsPanel.tsx:43-47 still calls getPendingInvites().then(...) with NO .catch/.finally — the recommendation to apply the same fix there is unmet, so a rejected invites fetch still pins that panel on 'loading' forever.
- **[auth-login] Secondary text links are sub-2.75rem tap targets** (P2) — login/page.tsx forgot-password/switch/footer links now display:inline-block with padding 0.5rem (436,490,504,508); but register.tsx footer Terms/Privacy links (483,487) have no display/padding, remaining small.
- **[auth-login] No portrait/landscape or iPad adaptation via useIsMobile/useIsCompact/useIsPortrait** (P3) — register/page.tsx:35 uses useIsMobile to tighten spacing/logo (272-289,391); login/page.tsx uses no responsive hook. Finding was flagged 'acceptable as-is', so remaining gap is minor/optional.
- **[auth-recovery] Passcode screen uses a completely different visual family from the other two Auth surfaces** (P1) — PasscodeEntry.tsx:56 now uses the identical auth gradient linear-gradient(165deg,#FAFAF7,#F2EDE7,#D4C5B2) as reset-password:209, and CTAs are now EMBER — but it still renders inside a TuscanCard variant='solid' (line 60) with a monospace 1.5rem input (lines 206-207) rather than the blurred rgba(250,250,247,0.85)/blur(20px) cream card used by the shared auth shell (reset-password cardStyle:218-230). Gradient + CTA divergences fixed; card treatment/input still differ.
- **[auth-recovery] Wing-only passcode shares 404 at the destination (/public/[slug] assumes a room)** (P1) — page.tsx getShareData() now handles share.room_id===null by loading the wing (lines 58-72), but that only feeds metadata. The gallery is populated by /api/public-share/[slug]/route.ts, which at lines 57-65 still does .eq('id', share.room_id).single() and returns 404 when !room — so a wing-only share still dead-ends at render time.
- **[auth-recovery] Error-alert palette is hardcoded and inconsistent across the two form surfaces** (P2) — Both form surfaces now render the same warm-red banner (#FDF2F2 bg / #FECACA border / T.color.error text) — forgot-password:141-148 and reset-password:139-147 — but the palette is still hand-hardcoded inline in each file rather than extracted into one shared token/style set as recommended.
- **[auth-recovery] Passcode empty/error state relies on JS-set border color with no reduced-motion or token-consistent focus** (P3) — PasscodeEntry.tsx:220-225 still drives focus/blur border color via onFocus/onBlur JS handlers (no CSS :focus-visible ring). There is no card entrance animation left to gate, so the reduced-motion concern is moot, but the JS-driven focus styling recommended to become a real :focus-visible ring remains.
- **[public-profile] Wing-count stats may double-count shared visits** (P2) — ProfilePageClient.tsx:189 relabels the stat as t('totalWingVisits') (recommendation option A), but it still sums w.visit_count across wings; no distinct owner-level/visitor DISTINCT count is computed.
- **[public-profile] Video thumbnails on public gallery cards autoload full video source with no poster fallback** (P2) — Core defect fixed: cards render only a play-icon poster placeholder and no <video>; <video> is mounted solely in the lightbox (PublicGallery.tsx:424-438,613). However thumbnail_url is still not added to the API select (route.ts:78) or PublicMemory type, so no real poster image is shown.
- **[pricing] Raw px / unitless sizing dominates /pricing, contradicting rem canon** (P2) — Key targets converted to rem (nav height '4rem' line 206; price fontSize '2.625rem' lines 688/710; touch min-heights '2.75rem'), but many decorative sizes remain raw px (fontSize 20/14/12, gap 12, borderRadius 20, padding '28px 24px' lines 619/590). Meaningful raw-px sizing still present.
- **[landing] Hero/scrim/shadow colors hardcoded as raw rgba umber instead of tokens** (P3) — Scrim tokens introduced (L.scrim.hero at 309, L.scrim.final at 1561) and CTA shadows tokenized (L.shadow.cta at 178, L.shadow.ctaHero at 1081; theme.ts:114-123). But raw rgba umber/gold still hand-typed at call sites: final-CTA boxShadow (1609), hover/active/glow shadows in the <style> block (779-780,802-810), and toggle-button rgba(36,28,21,0.55) backgrounds (324,358).
- **[tracks] Storage warning messages ignore used/limit/pct interpolation params — numbers never shown** (P1) — A separate Fraunces figure now renders the true pct (line 94 {Math.round(pct)}%), but the storageWarning50/80/95 strings across all 5 locales STILL hardcode 50%/80%/95% with no {used}/{limit}/{pct} placeholders (en.json:370-372), so the params passed at lines 96-100 are still dropped and the text can contradict the leading figure (e.g. "87%" next to "used 80%").
- **[tracks] StorageBanner has zero responsive handling and no reduced-motion / focus token** (P2) — useIsSmall() now drives column layout (StorageBanner.tsx:24,65-66) and the button has an onFocus gold #D4AF37 outline (line 122), so responsive + focus are addressed; however there is still no prefers-reduced-motion gate on the button's transform transition (line 118), unlike TracksPanel's @media reduced-motion block.
- **[legal] Off-canon background, ink and interactive colors throughout the legal surface** (P1) — Background now C.cream #FCFAF5 (canon) and headings C.inkSoft #403B36 (canon INK) — the two worst offenders fixed. But interactive/links still use C.terracotta #C66B3D (privacy p.104, linkStyle p.553) rather than canon EMBER #B85C38, and body copy uses C.walnut #8B7355 (theme.ts:6) rather than MUTED #716A5E, so the surface is still warmer-orange than canon.
- **[legal] Responsive layout uses clamp()/vw instead of the mandated useIsMobile/useIsCompact hooks** (P2) — security/page.tsx now imports and uses useIsMobile/useIsSmall (lines 5, 290-291) for nav/padding/grid branching. privacy and terms still rely purely on clamp()/vw (e.g. terms p.122 padding clamp) with no responsive hooks, so the pattern is not applied to all four pages.
- **[legal] Privacy/terms rely on fixed px sizing with no responsive hooks; only clamp() saves them at small widths** (P2) — px sizing was converted to rem (resolving the scaling half), but privacy/terms still add no useIsMobile hook to bump body copy on phones (privacy uses fixed 0.9375rem body via P(), p.514); the 0.75rem uppercase eyebrow (privacy p.99) is unchanged. The 'not blocking' polish item remains partially open.
- **[legal] Legal pages ignore useIsMobile/useIsCompact and rely on clamp()/vw for responsiveness** (P3) — security/page.tsx adopts useIsMobile/useIsSmall and nav icons are ≥2.75rem across all pages (privacy p.43). But privacy/terms/data-deletion still lack the useIsMobile/useIsCompact import, so the alignment is incomplete for the text pages.

## Per-surface status

- **atrium-relay-board**: ✅18 ⚠️3 ❌0 ⏸️0
- **atrium-nav**: ✅14 ⚠️4 ❌1 ⏸️1
- **atrium-discovery**: ✅17 ⚠️0 ❌0 ⏸️0
- **library-sidebar**: ✅18 ⚠️1 ❌0 ⏸️1
- **library-wall**: ✅19 ⚠️0 ❌0 ⏸️0
- **library-media**: ✅17 ⚠️3 ❌0 ⏸️0
- **explore-social-cards**: ✅15 ⚠️3 ❌1 ⏸️0
- **explore-main**: ✅14 ⚠️0 ❌0 ⏸️0
- **me-achievements**: ✅15 ⚠️1 ❌0 ⏸️0
- **me-stats**: ✅21 ⚠️0 ❌0 ⏸️0
- **me-identity**: ✅12 ⚠️0 ❌0 ⏸️0
- **tutorial-system**: ✅16 ⚠️1 ❌0 ⏸️0
- **tutorial-3d**: ✅11 ⚠️6 ❌0 ⏸️0
- **tutorial-menu**: ✅16 ⚠️1 ❌0 ⏸️0
- **onboarding-wizard**: ✅19 ⚠️1 ❌0 ⏸️0
- **settings-security**: ✅15 ⚠️2 ❌0 ⏸️0
- **settings-family**: ✅17 ⚠️3 ❌0 ⏸️0
- **settings-sharing**: ✅14 ⚠️5 ❌1 ⏸️0
- **settings-profile**: ✅15 ⚠️3 ❌1 ⏸️1
- **settings-shell**: ✅12 ⚠️2 ❌0 ⏸️1
- **settings-connections**: ✅17 ⚠️0 ❌0 ⏸️0
- **settings-notifications**: ✅15 ⚠️0 ❌0 ⏸️0
- **settings-legacy**: ✅19 ⚠️1 ❌0 ⏸️0
- **settings-subscription**: ✅20 ⚠️3 ❌0 ⏸️0
- **settings-cookies**: ✅16 ⚠️0 ❌0 ⏸️0
- **palace-touch**: ✅16 ⚠️3 ❌0 ⏸️0
- **palace-core**: ✅18 ⚠️0 ❌0 ⏸️0
- **palace-corridor**: ✅22 ⚠️1 ❌0 ⏸️0
- **palace-rooms**: ✅20 ⚠️0 ❌0 ⏸️0
- **import-hub**: ✅16 ⚠️2 ❌2 ⏸️0
- **kep-capture**: ✅16 ⚠️1 ❌1 ⏸️0
- **kep-rooms**: ✅17 ⚠️2 ❌0 ⏸️0
- **kep-landing**: ✅13 ⚠️0 ❌0 ⏸️0
- **interview**: ✅18 ⚠️0 ❌0 ⏸️0
- **family-tree-shared**: ✅16 ⚠️4 ❌0 ⏸️1
- **family-tree**: ✅20 ⚠️1 ❌0 ⏸️0
- **social-publish**: ✅18 ⚠️3 ❌0 ⏸️0
- **social-safety**: ✅16 ⚠️1 ❌0 ⏸️0
- **social-reactions**: ✅17 ⚠️1 ❌0 ⏸️0
- **visit-guest**: ✅19 ⚠️0 ❌0 ⏸️0
- **visit-room**: ✅18 ⚠️0 ❌0 ⏸️0
- **auth-login**: ✅19 ⚠️2 ❌0 ⏸️0
- **auth-recovery**: ✅14 ⚠️4 ❌3 ⏸️0
- **public-profile**: ✅18 ⚠️2 ❌1 ⏸️0
- **pricing**: ✅18 ⚠️1 ❌1 ⏸️0
- **landing**: ✅15 ⚠️1 ❌0 ⏸️0
- **landing-usp**: ✅13 ⚠️0 ❌0 ⏸️0
- **tracks**: ✅18 ⚠️2 ❌0 ⏸️0
- **legal**: ✅14 ⚠️4 ❌0 ⏸️0
