workflows:
  ios-release:
    name: iOS App Store Release
    max_build_duration: 30
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: Memory Palace ASC

    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: ai.thememorypalace.app
      node: 20
      vars:
        BUNDLE_ID: ai.thememorypalace.app

    scripts:
      - name: Install dependencies
        script: npm ci

      - name: Sync Capacitor iOS
        script: |
          mkdir -p out
          # Self-healing, always-tappable launch/error pages (single source of
          # truth: public/native-fallback.html). The old inline error page waited
          # 3s before its first connection probe and showed a bare spinner with no
          # retry button — reads as "not responsive after launch" on a slow start.
          cp public/native-fallback.html out/index.html
          cp public/native-fallback.html out/error.html
          npx cap sync ios

      - name: Copy app icons
        script: |
          cp -r memory-palace-ios-build/icons/* ios/App/App/Assets.xcassets/AppIcon.appiconset/

      - name: Add entitlements
        script: |
          cat > ios/App/App/App.entitlements << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>aps-environment</key>
            <string>production</string>
            <key>com.apple.developer.associated-domains</key>
            <array>
              <string>applinks:thememorypalace.ai</string>
              <string>applinks:www.thememorypalace.ai</string>
              <string>webcredentials:thememorypalace.ai</string>
              <string>webcredentials:www.thememorypalace.ai</string>
            </array>
          </dict>
          </plist>
          PLIST

      - name: Configure Info.plist (permissions + orientations)
        script: |
          PLIST="ios/App/App/Info.plist"
          set_string() {
            /usr/libexec/PlistBuddy -c "Set :$1 $2" "$PLIST" 2>/dev/null \
              || /usr/libexec/PlistBuddy -c "Add :$1 string $2" "$PLIST"
          }
          # Purpose strings must name the in-context use (Apple Guideline 5.1.1).
          set_string NSCameraUsageDescription "Take photos to place in the rooms of your 3D memory palace."
          set_string NSPhotoLibraryUsageDescription "Choose photos and videos to display in the rooms of your memory palace."
          set_string NSPhotoLibraryAddUsageDescription "Save images from your memory palace back to your photo library."
          set_string NSMicrophoneUsageDescription "Record voice notes and answer guided life-story interviews in your memory palace."
          set_string NSSpeechRecognitionUsageDescription "Transcribe your spoken answers during guided life-story interviews."
          # Marketing version must match the App Store listing (1.3.0); build number from Codemagic.
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 1.3.0" "$PLIST" 2>/dev/null || /usr/libexec/PlistBuddy -c "Add :CFBundleShortVersionString string 1.3.0" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" "$PLIST" 2>/dev/null || /usr/libexec/PlistBuddy -c "Add :CFBundleVersion string $BUILD_NUMBER" "$PLIST"
          # Auto-rotate: support portrait + landscape on iPhone.
          /usr/libexec/PlistBuddy -c "Delete :UISupportedInterfaceOrientations" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations:0 string UIInterfaceOrientationPortrait" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations:1 string UIInterfaceOrientationLandscapeLeft" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations:2 string UIInterfaceOrientationLandscapeRight" "$PLIST"
          # All four orientations on iPad (satisfies iPad multitasking).
          /usr/libexec/PlistBuddy -c "Delete :UISupportedInterfaceOrientations~ipad" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad:0 string UIInterfaceOrientationPortrait" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad:1 string UIInterfaceOrientationPortraitUpsideDown" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad:2 string UIInterfaceOrientationLandscapeLeft" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :UISupportedInterfaceOrientations~ipad:3 string UIInterfaceOrientationLandscapeRight" "$PLIST"
          # Orientations are unrestricted now, so UIRequiresFullScreen is not needed.
          /usr/libexec/PlistBuddy -c "Delete :UIRequiresFullScreen" "$PLIST" 2>/dev/null || true
          # OAuth deep-link return scheme. Registers ai.thememorypalace.app:// so the
          # Supabase in-app auth sheet can redirect straight back into the app via
          # appUrlOpen (see src/lib/auth/social-login.ts + src/lib/native/deep-links.ts).
          /usr/libexec/PlistBuddy -c "Delete :CFBundleURLTypes" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0 dict" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLName string ai.thememorypalace.app" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLSchemes array" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLSchemes:0 string ai.thememorypalace.app" "$PLIST"
          # Fail the build if any required key is missing — never ship a plist that triggers a 5.1.1/4 reject.
          for k in NSCameraUsageDescription NSPhotoLibraryUsageDescription NSPhotoLibraryAddUsageDescription NSMicrophoneUsageDescription NSSpeechRecognitionUsageDescription UISupportedInterfaceOrientations CFBundleURLTypes; do
            /usr/libexec/PlistBuddy -c "Print :$k" "$PLIST" >/dev/null 2>&1 || { echo "ERROR: Info.plist missing required key $k"; exit 1; }
          done
          echo "Info.plist permission strings + orientations configured."

      - name: Embed app privacy manifest
        script: |
          # Apple 5.1.1 / ITMS-91053: ensure an app-target PrivacyInfo.xcprivacy is
          # embedded. Capacitor ships only framework-level manifests, not an app one.
          # Best-effort and NON-FATAL — the build proceeds even if this step fails.
          set +e
          cp ios-privacy-manifest.xcprivacy ios/App/App/PrivacyInfo.xcprivacy
          gem install xcodeproj --no-document >/dev/null 2>&1
          ruby -e '
            begin
              require "xcodeproj"
              proj = Xcodeproj::Project.open("ios/App/App.xcodeproj")
              t = proj.targets.find { |x| x.name == "App" } or raise "no App target"
              grp = proj.main_group.find_subpath("App", true)
              unless grp.files.any? { |f| f.display_name == "PrivacyInfo.xcprivacy" }
                ref = grp.new_reference("PrivacyInfo.xcprivacy")
                t.add_resources([ref])
                proj.save
                puts "Embedded PrivacyInfo.xcprivacy into App resources"
              else
                puts "PrivacyInfo.xcprivacy already referenced"
              end
            rescue => e
              warn "privacy manifest embed skipped (non-fatal): #{e}"
            end
          '
          /usr/libexec/PlistBuddy -c "Print" ios/App/App/PrivacyInfo.xcprivacy 2>/dev/null || true
          exit 0

      - name: Set up code signing
        script: xcode-project use-profiles

      - name: Build iOS
        script: |
          cd ios/App
          xcodebuild build -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath $CM_BUILD_DIR/App.xcarchive \
            archive

      - name: Export IPA
        script: |
          xcode-project build-ipa \
            --workspace ios/App/App.xcworkspace \
            --scheme App \
            --archive-path $CM_BUILD_DIR/App.xcarchive

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
